# Optional Agent backend; never put these values in site/config.toml.
LLM_MODEL=your-model-name
LLM_API_KEY=your_api_key_here
LLM_ENDPOINT=https://api.openai.com/v1/chat/completions
LLM_MAX_TOKENS=2000
# Choose the listening port with: blogctl serve --port 3030
